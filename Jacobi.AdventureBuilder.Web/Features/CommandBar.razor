<FluentAppBar Orientation="Orientation.Horizontal">
    @foreach (var cmd in CommandInfos)
    {
        <FluentAppBarItem IconRest="new Icons.Regular.Size24.Navigation()" Text="@cmd.Name" OnClick="@(() => ExecuteCommandAsync(cmd.Id))" Tooltip="@cmd.Description" />
    }
</FluentAppBar>